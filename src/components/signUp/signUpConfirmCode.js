import React from 'react';
import { View, Text } from 'react-native';

const signUpConfirmCode = (props) => (
    <View>
        <Text>signUpConfirmCode</Text>
    </View>
);

export default signUpConfirmCode;
